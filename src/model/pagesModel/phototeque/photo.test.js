const {Photo} = require("./photo")
import {expect, jest, test,} from '@jest/globals';
import { VALIDATORS } from '../../utils/Validators';


test("Let's make a fake test",()=>{
    // const photo = new Photo()

    expect(2 == 2).toBe(true)
})